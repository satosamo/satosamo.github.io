<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Matej Lorinc</title>
    <style>
        /* Simple CSS for centering */
        body {
            font-family: sans-serif;
            display: flex;
            flex-direction: column;
            justify-content: center;
            align-items: center;
            min-height: 100vh;
            margin: 0;
        }
        .link-list {
            list-style: none;
            padding: 0;
            text-align: center;
        }
        .link-list li {
            margin: 15px 0;
        }
        .link-list a {
            font-size: 1.5em;
            color: #007bff; /* Blue link color */
            text-decoration: none;
        }
        .link-list a:hover {
            text-decoration: underline;
        }

        .centered-text {
        display: block;          /* Makes the <i> element behave like a block element */
        text-align: center;      /* Centers text horizontally */
        white-space: normal;     /* Allows text to wrap to next line */
        word-wrap: break-word;   /* Breaks long words that overflow */
        max-width: 100%;         /* Ensures it doesn't exceed parent width */
        overflow-wrap: anywhere; /* Handles extremely long words without spaces */
        }
    </style>
</head>
<body>
    {{ content | safe }}
    <br/><br/><br/>
</body>
</html>